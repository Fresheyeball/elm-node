module Http.Agent where

{-| write later

-- Class: http.Agent
-- new Agent([options])
-- agent.destroy()
-- agent.freeSockets
-- agent.getName(options)
-- agent.maxFreeSockets
-- agent.maxSockets
-- agent.requests
-- agent.sockets -}

foo = 1
